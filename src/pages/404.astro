---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Heading from '@/components/atoms/Heading.astro';
import Paragraph from '@/components/atoms/Paragraph.astro';
import Button from '@/components/atoms/Button.astro';
import { localizedUrl } from '@/lib/i18n';

// La 404 se sirve en español desde cualquier ruta (incluidas las /en/), porque
// Apache la devuelve como error document único y no sabemos el idioma pedido.
const locale = 'es';
---

<BaseLayout title="Página no encontrada" locale={locale} noindex={true}>
  <section class="bg-black flex-1 flex items-center justify-center min-h-[70vh] px-4 text-center">
    <div>
      <p class="text-[120px] md:text-[200px] font-black italic text-white/5 leading-none select-none">404</p>
      <Heading level={2} class="text-white mb-4 -mt-8 md:-mt-16">PÁGINA NO ENCONTRADA</Heading>
      <Paragraph class="text-secondary mb-8 max-w-md mx-auto">
        La página que buscas no existe o ha sido movida.
      </Paragraph>
      <div class="flex justify-center gap-4 flex-wrap">
        <Button variant="primary" href={localizedUrl('/', locale)}>VOLVER AL INICIO</Button>
        <Button variant="outline" href={localizedUrl('/contacto/', locale)}>CONTACTAR</Button>
      </div>
    </div>
  </section>
</BaseLayout>
