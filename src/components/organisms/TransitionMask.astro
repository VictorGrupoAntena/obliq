---
interface Props {
  enterColor?: string;
}
const { enterColor = 'black' } = Astro.props;
---

<div
  id="transition-mask"
  class:list={[
    'fixed inset-0 z-[999] pointer-events-none translate-y-full',
    enterColor === 'white' ? 'bg-white' : 'bg-black',
  ]}
  aria-hidden="true"
/>
